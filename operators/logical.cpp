#include<iostream>
using namespace std;
int main(){
    bool a = true;
    bool b = false;

    cout << "a && b is " << (a && b) << endl;
    cout << "a || b is " << (a || b) << endl;
    cout << "!a is " << (!a) << endl;
    
    return 0;
}